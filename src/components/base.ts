import { css, LitElement, CSSResultGroup } from "lit";
import { customElement, property } from "lit/decorators.js";

export class QBase extends LitElement {
        static styles: CSSResultGroup = css`:host, :host * {box-sizing: border-box;}`
}
