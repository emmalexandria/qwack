import type { Args, Meta, StoryObj } from '@storybook/web-components';

import './icon.ts'

const meta: Meta = {
  title: "Atoms/Icon",
  component: "q-icon",
  args: {
    icon: "home"
  }
}

export default meta;

export const Icon: StoryObj = {

}
